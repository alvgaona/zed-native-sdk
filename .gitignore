**/.DS_Store

# Local agent context — not part of the published extension
.claude/
CLAUDE.md

# Grammar checkouts and compiled wasm, written by Zed when it builds the extension
grammars/
*.wasm
target/
